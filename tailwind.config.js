module.exports = {
  content: ["./src/**/*.{html,js,ts,jsx,tsx}"], // Adjust paths as per your project
  theme: {
    extend: {
      colors: {
        primaryBg: "var(--primary-bg)",
        primaryText: "var(--primary-text)",
      },
    },
  },
  plugins: [],
};
